<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/menu_search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        android:title="@string/search"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/menu_clear_search"
        android:title="@string/clear_search"
        app:showAsAction="never" />

    <item android:id="@+id/menu_reload"
        android:title="@string/reload"
        app:showAsAction="never" />

    <item android:id="@+id/menu_toggle_polling"
        android:title="@string/start_polling"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_manual_check"
        android:title="Manual Check"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_alarm_clock"
        android:title="@string/alarm_clock"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_setting"
        android:title="@string/photo_setting"
        app:showAsAction="never"/>

</menu>